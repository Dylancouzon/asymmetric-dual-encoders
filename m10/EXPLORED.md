# M10 closed avenues, each with what would reopen it

The register is `m10/PLANNING.md` §7 (considered and rejected, with reopening conditions) and
`m10/COV_CANDIDATES.md` (refused selection components). This file lists what changed after the
plan was written, so a future session does not re-derive it.

| avenue | why closed | reopens if |
|---|---|---|
| FineWeb, any role | ruled 2026-09-01: documents need reserved-set fingerprints that do not exist; seeding adds a rights review for topics Wikipedia and the pool already carry | family A wins on forms yet COV shows a topic gap Wikipedia cannot seed |
| A student above 35M | hard cap, Dylan 2026-09-01 | never |
| A nonlinear head **after pooling** | no fastembed serving path: fastembed pools the graph's per-token output itself and a nonlinearity does not commute with the mean. **Corrected 2026-09-04b:** this never applied to a head applied **per token before pooling**, which ships exactly (`results/m10_head_mlp_parity_box.json`: residual form, min-cos 0.99999989, zero custom ops, 34.96M) — screen arm **G-MLP** (amendment B3) | a post-pooling head reopens only if fastembed gains a post-pooling slot |
| Claude (Sonnet/Haiku) as the synthetic-query generator | Anthropic's terms restrict using the Services to train competing models; the M7 licensing rule requires an open-weights generator with a pinned revision; a usage window is not a batch pipeline | never |
| Climate-FEVER as a claim-form selection surface | no licence at the primary source (the HF tag is a wrapper); same finding as M7 | a licence statement appears at github.com/tdiggelm/climate-fever-dataset |
| LegalBench ConsumerContractsQA | CC BY-NC 4.0 at the primary source | **REOPENED 2026-09-04** by Dylan's rule that non-commercial licences are admissible for validation but not training (`research/m7-data-licensing.md` §Rule change 2026-09-04). It is a selection surface, never training data, so it is re-admissible; it restores the legal family's second component (three family IDs without LEDGER, one legal family) |
| Reserved-set document fingerprints | an unspent held-out access in disguise (Codex pass 1 B2) | never inside M10 |
| Spending M9's LoTTE read on the M9 candidate | would burn the only fresh surface on a candidate that misses | never |
| The RTX 3080 box as M10's execution target | Dylan 2026-09-01: "M10 will be done on a GPU budget, if allowed, or not at all." A LEAF-scale build is ≈ 10 days on it; the 50M dose it forced was a box artifact | **PARTLY REOPENED 2026-09-04** (amendment A7): measured, the box runs the M10 recipe at 683 examples/s blended against the plan's assumed 560 on a rented A100, because the step is launch-bound (`results/m10_rate_bench_box.json`, PLANNING §11). The ruling stands where it bites — no dose or screen size is set by the box's wall-clock — but screens, confirmations, COV work and M10.0-c run on the box, which also holds ~200 GB of M9 caches. ~~Generation still needs the cloud (Qwen3-8B bf16 is 16.4 GB on a 10 GB card)~~ — **superseded by decision 14: generation runs on the box with Qwen's official AWQ release** |
| A 50M-example build with an 83.4M cap | set by the box's wall-clock; LEAF's dose is 201M on an easier target. Now 200M, extension capped by budget | never — the dose is budget-bound |
| ~~Qwen3-8B 4-bit as a generation artifact~~ | **REOPENED and adopted 2026-09-04 as decision 14:** Qwen's *official* AWQ release (`Qwen/Qwen3-8B-AWQ`, pinned revision) on the box via vLLM; the bf16-only rule was written for an 80 GB card, not for quality, and the smoke rubric plus the A8 diversity gate are the quality guards. Community quants and the Qwen3-4B fallback stay out | — |
| A cosine-space phase-1 loss as a family-D arm (pyNIFE's headline recipe claim, github.com/stephantul/pynife) | closed by algebra, not measurement: the head ends in L2 normalize and the teacher vectors are unit-norm, so squared L2 = 2 - 2cos and the two objectives share a gradient up to a factor of 2 (`m9src/nano.py:6`). pyNIFE's static student is unnormalized at the loss, which is where its MSE-vs-cosine gap lives. **Pointer corrected 2026-09-04 (Fable MINOR):** `nano.py`'s `forward` returns the *unnormalized* head and the normalization happens in the training loop, `m9src/longrun.py:777`, so the algebra is pinned there, not to the module docstring | the student output stops being normalized before the loss |
| A ranking-aware phase-2 loss (KL over mined candidates; InfoNCE on the seed passage) as a family-D arm, and with it the 1M-document bank, the exact-mining pass, the HNSW fallback, the τ entropy rule and the seed-rank field | cut 2026-09-04 (mandate amendment A1) on cost and sufficiency — **corrected the same day: LEAF's Appendix B is intermediate-layer KD, not a ranking term; no published null exists for this class** — because every comparable asymmetric system (LEAF 97.7%, EmbedDistill, arXiv 2306.11550, mxbai-edge-colbert, DistilVDR) trains by pure embedding regression. It was the least-supported and most machinery-heavy block in the plan | only as its own milestone, never mid-M10, and only if families A and G both land and the annealed plateau still misses the release bar by **less than 0.01** |
| 3.0M generated queries at 250K per form across all twelve | cut to ≈1.0M for the six non-harvestable forms 2026-09-04 (amendment A2): three independent saturation curves put diminishing returns under ~1–1.5M (DistilVDR above 75% of 1.49M; SPEED log-linear to ~920K; doc2query 90–95% of max at 50–75% coverage), and the harvestable forms come from real pool text instead. PLANNING §12 | screen contrast A4−A3 resolves strongly AND the plateau is still rising at the dose cap |
| The second full-dose build seed (decision 8, ≈100 GPU-hours) | withdrawn 2026-09-04 (amendment A5): descriptive by construction, could trigger no action. Replaced by a screen-dose seed pair on the selected recipe; the hours go to extension cycles | the ceiling is raised and every extension cycle has already run |
| Dropping family F to anchor and build on MiniLM-L6 | **proposed and withdrawn in the same review, 2026-09-04**: it would also have killed family C (M9's candidate is a bge-small student, so a MiniLM build has no warm start), and arXiv 2306.11550's depth curve (1/2/4 layers → 86.1/92.5/96.2%) disagrees with LEAF's success on 6 layers. A 5M arm ≈ 2 GPU-hours is not where to economise when it picks the build student. Amendment A6 reorders F to run second instead | never in that form |
| The COV resolution check as a screen-cutting **selection** rule | Codex pass 7: its comparators were family F's backbones and it made a COV-read decision before selection. Demoted to a descriptive resolution number with non-candidate comparators (e5-small-v2, gte-small) | never as a selection rule. **A4's sizing role (MDE = max(0.0056, distance); admit LEDGER, then α = 0.05) was adopted and STRUCK the same day by the Codex pass** — an α that adapts to an observed width is not pre-registration. The number is a power disclosure only; MDE 0.0056 and 0.025/13 are fixed |
| MedlinePlus / CDC text as consumer-health harvest | US public domain, but they are MedQuAD's sources: the MedicalQA COV read would reward arm A3 by construction (Codex 2026-09-04 M7) | MedicalQA leaves COV |
| ClinicalTrials.gov as harvest text or a `ctgov-title` surface | its terms clause could not be read (JS page); NLM's government-works policy is not the site's own grant | the clause is recorded |
| CUREv1 as a selection-bearing COV family | pools annotated by Qwen 2.5 72B; PubMed-family provenance that fingerprints cannot fully see (Codex M6). Decision 12 asks only for a reported diagnostic | Dylan rules otherwise |
| G-MLP as a bottleneck `1152→512→GELU→1024` | output rank ≤512 — it traded rank for nonlinearity (Codex M3); replaced by the residual form, which keeps the linear head's full rank | never |
| **`ROUTE_WIDE`, the widened keyword router** | its registered judged-precision gate read **28%** on health's newly-admitted marginal and **38%** on finance's, against ≥ 80%. The router selects on the PRESENCE of "medic\*"/"hospital"/"financial", not on subject, so the marginal is mostly biographies and institutions. `draw()` is back on T2-3's `ROUTE` | never as a widening. The subject-level ladder (T2-8) is the replacement |
| **A subject filter as the fix for the SEED-SUPPLY shortfall** | closed by arithmetic, before it was built: a precision filter cannot create seeds. Health's widened marginal has a usable ceiling of ~17.6K against a ~33K need, so the filter's best case still misses. **Supply is closed by corpus size** (`wikipedia-body`, 72,826 health seeds) and precision by the filter — two problems, two levers, and conflating them cost a day of planning | never |
| **"The generator absorbs seed noise", from 59% seed precision vs 84% smoke on-form** | withdrawn 2026-09-05 on a Fable pass, and **it was not a close call**: the smoke's seeds were the top-40 keyword scorers of a 400K pool (T2-3's own disclosed non-representative draw) and the gate's control was uniform over the entire 8,663/19,466 store, so the pair is (top tail, uniform population). **T2-7 then measured the real quantity**: on-form given an off-subject seed is 0.50–0.66 against 0.83–0.94 given an on-subject one — seed precision propagates but does not determine. Kept because the failure mode is the lesson: two numbers from two populations, compared because they were both to hand | never in that form; the linked measurement replaced it |
| **Admitting a seed store on its on-form rate after failing the seed-precision gate** | refused 2026-09-05 by the Fable pass as the change W6 forbids: *"attaching admission to a different, kinder-looking instrument after the registered one returned FAIL is a protocol change after observation"*. T2-7 runs report-only and licenses exactly one action, which is not admission | only Dylan, by ruling W6 |
| **A fourth instrument aimed at the seed-store question** | registered stop rule (T2-7 item 7): `ROUTE_WIDE`'s gate, `wikipedia-body`'s gate and the on-form diagnostic are three, and the session brings no fourth | Dylan asks for one |
