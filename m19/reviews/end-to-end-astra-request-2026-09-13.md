# M19 fresh Astra end-to-end review request

Independently review `m19/reviews/end-to-end-scope-v1.json` at implementation commit `b0faad1` and
return `GO`/`NO-GO` for real development pool freezing and blinded judgments. Focus adversarially on
inheritance/path guards, candidate math/export/loader, exact artifact retrieval, seven-route pool
construction and blinding, judgment independence/audit/agreement, frozen metrics/eligibility and the
synthetic interrupted confirmation transaction. Check the actual cost-only pilot without judging
relevance or inspecting route quality.

Read only the scope files, directly corresponding tests, named M19 bundle/pilot pool/packet if
needed, `CLAUDE.md` and `instructions-m19.md`. No confirmation content, protected/spent evaluation
surfaces, recursive `work/`/`results/` search, network, edits, relevance labels or quality metrics.
Report P0–P3, smallest fixes, exact commit/scope hash, tests and full access declaration.
