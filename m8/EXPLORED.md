# M8 closed avenues

One row per closed avenue: what was tried, why it is closed, and what would reopen it. A closed
avenue with no reopening condition is a guess, not a finding.

| avenue | why closed | what would reopen it |
|---|---|---|
| **The cheap doc-side head (menu item D1)** | `E14-HEAD` measured it: LIN −0.0244, MLP −0.0293 dense against a +0.0040 bar, all six arms agreeing in sign, on a patch stack that measured as a clean null. §5.4 also excludes `doc_side_head` from `qualifying_table`, so it could never have opened the release path alone. | Only a 5,000-step reported set with a paired 5,000-step R0N — LIN's registered label is OPTIMIZATION-INADEQUATE, so it is not *formally* closed. That is ~9 training arms and ~9 scoring passes: a spend decision, not a free follow-up. |
| **Phase-A data volume as a route to the bar** | `B3`: a 4× dose moves dense +0.00135; the fitted slope needs ~17.6× the pool (~5.9M pairs) to reach 0.0040. M7's entire MS MARCO addition was 490K. | A B-side pair lever — `B3` held the Phase-B checkpoint fixed. `D-FINEWEB` is that case, and is blocked on a pool-varying floor that does not exist. |
| **The recipe/data class as the route to the bar** | Not a single probe — a measurement carried in from M7: its entire post-gate lever programme transferred to the six at **0.000 ± 0.005**, and the clean-stack tax priced half a million extra real pairs at +0.0058 unresolved. §7 therefore budgets the class as hygiene. Deferred behind `D2` by the 2026-08-29 audit; rows keep their `plan_status`. | A capacity lever clearing its bar, so that Stage R has something to assemble around. |
| **Spending M8's confirmatory access on a repeat of M7** | Pre-committed exit (§15, 2026-08-29): with every measured lever dead, P(ship) under the `m7_repeat` scenario is **0.002**, so the access re-publishes M7's miss while permanently costing M9 its clean sets. | `D2` or `B10` clearing its bar — or Dylan overriding, before D2's number exists. |
| **B11 / fusion complementarity** | NOT closed by E11, as the plan's v1 wrongly said — C1 is fused-vs-fused, so fusion is live for the PRIMARY leg. Closed instead by the frozen fusion operator's own no-routing rule, plus the optics of a query-dependent weight. | Registering a length-conditioned fusion family as THE fusion operator, before Stage R. After Stage R it is out for the milestone. |
| **Direct dense-Gram ridge at V ≥ 50K** | 20.3 GB fp64 at 50,368 rows against an 18 GB budget — the arithmetic that closed granite-r2 and gte-modernbert in M7. | **REOPENED 2026-08-29 and confirmed**: `m8src/blockcg.py` never forms the Gram; B7 solves 64K in 10 s / 4.4 GB and 128K in 17 s / 5.7 GB. The closure was an artifact of the solver, not of the problem. |
| **Unpreconditioned CG on a Zipfian Gram** | Does not converge in 1,500 iterations (5.9e-4). Real token frequencies span orders of magnitude, so the Gram's spectrum is extremely skewed. | Nothing — Jacobi preconditioning (61 iterations) is the fix and is in the solver. Recorded because a UNIFORM synthetic converged in 131 and would have produced a feasibility PASS that said nothing about the real problem. |
| **T1 in a single shared student frame** | M7's shared bag matrix works only because all ten registered encoders ship a byte-identical `bert-wordpiece-30522` vocabulary. None of T1's four challengers does. | Nothing: it is a fact about the candidates. The frame is now fixed *within* a tokenizer family and cross-family screens are labelled teacher-plus-tokenizer. |
| **granite-r2 and gte-modernbert as teachers** | Screened for the first time (M7 closed them on arithmetic, not merit). Both lose CI-resolved to the incumbent: −0.052 and −0.109 on the closed-form table criterion, every optimum interior. | Nothing on this criterion — the gap is 5–11x the 0.0096 swap penalty. A different criterion would need its own registration and Dylan's sign-off. |
| **LoTTE `search` queries** | Non-commercial-research-only, inherited from the GooAQ licence (paper Appendix D, quoted in LEDGER §2.3). | A licence change, or a ruling from Dylan that a non-commercial research measurement is acceptable for an internal shadow gate — the same class as M7's clean-stack-tax arm. |
| **PMC-OA** | E8: its unique value is duplicated by cleaner sources. | Only one condition: the genre probe showing a biomedical-specific gap. |
| **ICT pair augmentation** (was B3's lever) | Retired before any arm ran. "Equal updates AND equal exposure" over-constrains a fixed batch, and the ICT shortcut survives sentence removal because the positive's teacher vector is precomputed over the FULL document — the cheap repair does not work. Above all it measures whether Phase-A budget spent on ICT helps, not whether pairs are scarce. Registry row `B3-ICT`, refused. | Re-encoding one ablated context per pair with the teacher (the expensive repair), plus a design that separates the estimand from the budget question. Nothing cheaper. |
| **Phase-A pair-count levers** (ICT, doc2query, more real pairs) | **MEASURED DEAD by B3.** A 4x dose of distinct pairs at fixed compute moves the dense scalar +0.00135 and the fused +0.00369 against a 0.0040 bar; the fitted slope is +0.00097 per doubling, so reaching the bar needs ~17.6x the pool (~5.9M pairs). M7's entire MS MARCO addition was 490K, under 1.5x. | A **B-side** pair lever — B3 held the Phase-B checkpoint fixed and its scope explicitly excludes it. `D-FINEWEB` is exactly that case, which is why B3's result does not close it. |
| **MS MARCO in the release stack** | Non-commercial-research-only terms (IBM Granite precedent), and the exclusion was **priced**: +0.0058 [−0.0015, +0.0131] on the six and **−0.0030** [−0.0066, +0.0003] on dev — positive on one endpoint, negative on the other, neither resolved. The clean stack is free. | A licence change. Note the research-only arm exists (`m7src/cleanstack_tax.py`) and `freeze.assert_releasable` refuses its output by design. |
| **harrier as a teacher** | CLOSED by Dylan 2026-08-29 on **undisclosed training data**: no comparison design repairs not knowing what a teacher has read, and the evaluation protocol is the one thing not relaxed. Two lesser blockers behind it (last-token pooling needing new code; no published retrieval number to check a screen against). | Disclosure of its training data. The vendor rule was never the obstacle — Microsoft passes "OK with justification". |
| **CQADupStack subforums as the shadow** | Rejected 2026-08-29 **not on contamination — they passed that screen — but on correlation with the exam**: two of the reserved four confirmatory sets ARE CQADupStack, so iterating against subforums would tune toward a confirmatory read we cannot re-take. | Nothing for the shadow role. They remain usable for a purpose that is not protecting the reserved four. |
| **LoTTE at slice granularity** | The slice-level bar dropped all ten. Superseded by Dylan's per-question ruling: seven clean-community slices survive at ~14,034 queries. Three stay dead on community overlap (`writing/test`, `science/test`, `technology/test`) and no remedy applies to those. | Nothing — the three dead slices overlap protected communities, which per-item removal cannot fix. |
| **A LINEAR doc-side head** | Provably absorbable into the table (standing directive #4), so it adds no capacity and would measure nothing while producing a number. `E14-HEAD` therefore specifies a NONLINEAR head. | Nothing — it is algebra, not a measurement. |
| **B17's ≤0.40 "class caps in domain" branch** | Fired at 0.1999 and was **disowned**: the same class fitted on 350K general queries scores 0.3439, so B17 measured its own 957-query fit set, exactly as its pre-registered caveat warned. The rule was NOT amended. | A capacity probe with a fit set large enough that its own fit is not the binding constraint. |
| **Post-hoc linear projection into a frozen doc space** | M2/M7 negative result, airtight: potion-32M→arctic-m reaches 0.3280 against its own symmetric 0.3427, even with test-set-tuned regularisation. | Nothing linear. This is the observation that motivates E14's *nonlinear*, *jointly trained* framing. |

- **Compositional new rows — BOTH classes closed on evidence (`D2-PRE`, 2026-08-29).** Non-overlapping
  multi-word segmentation AND additive overlapping word/character n-gram rows, at an equal 35,014-row
  budget on frozen incumbent rows: every arm NEGATIVE out-of-fold (best −0.0028, D2's own −0.0052)
  against +0.00519. Not a coverage failure (zero-update mass 0.0001–0.001 vs a 20% gate), not a
  compile failure (sum-init reproduces R0 to −2.8e-06), not leakage (0/0 overlap), not the lambda
  rule (interior for 3 of 4 arms). **Decisive detail:** zeroing 23,601 of 35,014 rows moves the
  result 0.0002 — two-thirds of the added vocabulary is inert. §17b's fertility mechanism DID move
  (reduction 0.164/0.176) and the metric did not follow, which is direct evidence for its downgrade
  to correlated headroom. Closes `D2` and the n-gram class; `§13`'s supersession question is moot.

- **Doc-centroid targets (`B8`, 2026-08-29).** Fitting the table to the positives' centroid instead
  of the teacher's query vector costs **−0.167** on the dev group vector; a 50/50 blend is −0.003.
  The "retrieval scores q·d, never q·q, so aim at the document manifold" intuition is REFUTED, not
  untested: the teacher's query encoder already does that mapping, and a centroid target fits which
  documents happen to be positive. Keep R0's bare target.
- **Train-free dense PRF (`VECTOR-PRF`, 2026-08-29).** Published config, no grid: **−0.051** dev
  group vector, **−0.021** fused, negative on all six components. Closes query-side post-hoc
  refinement. Does NOT close a learned feedback encoder (out of scope under E1 regardless).
  Together with `D2-PRE` this says the query-side gap is neither a vocabulary-resolution problem nor
  a query-placement problem.
