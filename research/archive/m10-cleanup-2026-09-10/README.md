# M10 records before the scope cleanup

These documents are byte-for-byte copies from commit `46ea18d`, before the
2026-09-10 cleanup requested by Dylan. `MANIFEST.json` records their SHA-256 hashes.
Git history remains the provenance record; this archive preserves the long registrations,
owner rulings, withdrawn claims and review dispositions without making them the daily runbook.

Paths and milestone numbers inside these copies use the old layout. Read the repository-root
`ROADMAP.md` for current ownership. The screen registry, result JSONs and frozen evaluation
artifacts remain at their original paths; archiving prose changes no experiment or access rule.
