- **P2 — Cached legacy `train` survives the fix.** [m17src/common.py:26](/home/dylan/asymetric-dual-encoders/m17src/common.py:26): the helper changes `sys.path` only. If the failed parity invocation is retried in the same interpreter, its legacy `sys.modules["train"]` is reused at [prepare_data.py:1774](/home/dylan/asymetric-dual-encoders/m17src/prepare_data.py:1774), reproducing the same `AttributeError`. Reproduced with a synthetic cached module. A fresh CLI process avoids this particular failure.

- **P2 — Regression test cannot detect removal of the production fix.** [m17src/test_prepare_data.py:874](/home/dylan/asymetric-dual-encoders/m17src/test_prepare_data.py:874): the test invokes the helper directly and never calls `stage_protected`; deleting the production call leaves it green. It also passes with legacy `train` cached, which I reproduced, because `PathFinder` bypasses `sys.modules`. A production-call-site test is feasible using an intercepted `protected10` import and a stub `build()` that stops before payload access; retain the separate draft-registry gate test.

**Verdict:** Executed-half clearance remains unresolved: completing Q1–4 requires the additional dependency/downstream source reads requested above, which the current allowlist excludes.
