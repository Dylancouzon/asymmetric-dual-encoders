- **P2 — `protected10.build()` can undo the path repair.** At [prepare_data.py:1258](/home/dylan/asymetric-dual-encoders/m17src/prepare_data.py:1258), repair happens before `build()`. On an index-cache miss, [protected10.py:66](/home/dylan/asymetric-dual-encoders/m10src/protected10.py:66) lazily imports `cov_screen`, whose [line 20](/home/dylan/asymetric-dual-encoders/m10src/cov_screen.py:20) puts `m7src` first again. With `train` uncached, parity consequently resolves the legacy driver even after both fixes. Reproduced using the actual source statements and an isolated import stub, without payload access. Reassert after `build()` as well, and cover path mutation *during* `build()` in the regression test. This affects another screened rebuild; the completed resume skipped `protected`.

**Q1:** The complete shared-basename list is `train` (`m7src`, `m17src`) and `run_arm` (`m7src`, `m10src`); none spans all three. `common`, `cache`, `vocab`, `export`, and `evaluate` have no legacy counterparts in these trees. Repository dependencies read from `protected10`: `m7src/decontam.py`, `m10src/cov_admit.py`, and the lazy `m10src/cov_screen.py`. The requested `m10src/decontam.py` does not exist.

**Q2:** No shared-basename import was found before parity in the reviewed stage/dependency source. `common`, `cache`, and `vocab` are bound before screening at [prepare_data.py:57](/home/dylan/asymetric-dual-encoders/m17src/prepare_data.py:57). Subsequent stage imports are `teacher`, `hashing`, `table`, and third-party modules; the first stage import of `train` is [parity:1774](/home/dylan/asymetric-dual-encoders/m17src/prepare_data.py:1774). The six reused markers are sound with respect to this import-order change.

**Q3:** No active comparison retains the old builder hash. [lock.py:278](/home/dylan/asymetric-dual-encoders/m17src/lock.py:278) compares the manifest hash with the **amended** invariant; current source hashes to `aefcf585…`. Training checks executed artifact identities and recipe, while export/evaluate have no old-builder-hash gate. [lock.py:92](/home/dylan/asymetric-dual-encoders/m17src/lock.py:92) excludes the entire `lock` block from registry identity. The [amendment:289](/home/dylan/asymetric-dual-encoders/m17/registry.json:289), recording date, old/new hashes, reason, and original commit, is sufficient under the dated-amendment rule.

**Q4:** None of the executed/downstream paths reads `build_record.json` or refuses its `partial rebuild` label. That timing requirement belongs to pre-half `measured_allocation`. Ledger preparation cost must include both invocations, preserving the original clock start. The crash’s stage timers total **339.910 s**, but that is only a lower bound: [protected10.build():1259](/home/dylan/asymetric-dual-encoders/m17src/prepare_data.py:1259) runs outside those timers. Adding that subtotal to **3,779.4 s** would still omit work.

**(a) Proceed:** Yes, the reported completed resume may proceed to `lock.py --phase executed`.

**(b) Fix timing:** No required fix before that command; repair the P2 before another screened rebuild and account for both invocations in the ledger.
