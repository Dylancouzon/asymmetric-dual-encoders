# Codex gpt-5.6-sol adversarial review #2, 2026-08-26 evening (post-blocker-fix session)

Verbatim findings; brief: scratchpad codex_brief.md (teacher swap + B2/B3/B5/B6 fixes + protocol).
Dispositions tracked in m7/LEDGER.md.

One-line positive: the one-sided directions, add-one Monte Carlo construction, and three-test Holm plumbing are internally coherent if the sharp exchangeability null is accepted.

1. **BLOCKER — the B2 bank mask can make training index past the end of the bank.** [train.py:408](/home/dylan/asymetric-dual-encoders/m7src/train.py:408) sets `nb`, removes banned `bank_ids` without updating it, then samples indices from `[0, nb)` at [train.py:515](/home/dylan/asymetric-dual-encoders/m7src/train.py:515) and [train.py:538](/home/dylan/asymetric-dual-encoders/m7src/train.py:538). Falsifiable claim: if even one of the sampled two million rows is banned, some legal sampled indices exceed `len(bank)-1`, eventually causing an out-of-range CUDA index.

2. **BLOCKER — the folded release artifact is not on the gate/freeze/final path.** `save_release` is defined at [table.py:200](/home/dylan/asymetric-dual-encoders/m7src/table.py:200) but has no caller; G4 loads the ordinary unfolded checkpoint at [gate.py:34](/home/dylan/asymetric-dual-encoders/m7src/gate.py:34), and `freeze.write` freezes that same `work/runs/<id>.npz` at [freeze.py:36](/home/dylan/asymetric-dual-encoders/m7src/freeze.py:36). Falsifiable claim: following `STATUS.md` today never creates, gates, freezes, or scores the folded artifact despite [STATUS.md:20](/home/dylan/asymetric-dual-encoders/m7/STATUS.md:20) claiming otherwise. Also, `save_release` compares two fp32 in-memory models before writing; it never reloads either serialized fp16 or int8 variant.

3. **BLOCKER — the Stella re-gate silently reuses the BGE capacity probe.** [gate.py:91](/home/dylan/asymetric-dual-encoders/m7src/gate.py:91) always reads the generic `m7_capacity_probe_noprefix.json`, whose contents carry no teacher identity at [m7_capacity_probe_noprefix.json:1](/home/dylan/asymetric-dual-encoders/results/m7_capacity_probe_noprefix.json:1). Falsifiable claim: `M7_ENCODER=stella-400M-v5 gate.py s1-objB` can pass G2 using the old 768-dimensional BGE document space without detecting the mismatch.

4. **BLOCKER — the “4-gram short-query fix” still misses short queries embedded in normal documents.** Protected 4–7-word queries contribute word-4-grams at [decontam_pool.py:49](/home/dylan/asymetric-dual-encoders/m7src/decontam_pool.py:49), but a pool document contributes 4-grams only when the document itself has fewer than eight words at [decontam_pool.py:108](/home/dylan/asymetric-dual-encoders/m7src/decontam_pool.py:108). Falsifiable claim: a 20-word pool document containing a protected five-word query verbatim produces only rolling 8-grams and is not banned. One-to-three-word queries remain whole-document-exact only.

5. **BLOCKER — final tier assignment violates the binding CI rule.** The mandate says tiers are decided only by paired-bootstrap CIs and unresolved comparisons fall lower at [instructions-m7.md:28](/home/dylan/asymetric-dual-encoders/instructions-m7.md:28); [final_run.py:205](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:205) and [final_run.py:227](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:227) assign rejections/tiers solely from sign-flip Holm decisions. Falsifiable claim: a Holm rejection with a percentile CI crossing zero is currently reported as a tier win, contrary to the mandate.

6. **BLOCKER — the six-set access breach is not curable by disclosure under the project’s own norms.** The mandate limits class-(a) access to three named harness cells at [instructions-m7.md:66](/home/dylan/asymetric-dual-encoders/instructions-m7.md:66); the ledger admits the FiQA throughput breach at [LEDGER.md:283](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:283), and [SIX_ACCESS.log:1](/home/dylan/asymetric-dual-encoders/m7/SIX_ACCESS.log:1) records another full-six BM25 recomputation outside that named validation. [CLAUDE.md:146](/home/dylan/asymetric-dual-encoders/CLAUDE.md:146) forbids retroactive protocol relaxation. Falsifiable claim: an audit log proves access occurred; it cannot make the earlier accesses authorized or restore a “single final access” claim.

7. **BLOCKER — the recorded next-step order reaches freeze before mandatory work.** [STATUS.md:26](/home/dylan/asymetric-dual-encoders/m7/STATUS.md:26) schedules objective B, the LR band, fusion, then freeze, omitting the three inits, two prefix variants, flat-vs-learned weights, released-table ANN sweep, and cost measurements required at [instructions-m7.md:70](/home/dylan/asymetric-dual-encoders/instructions-m7.md:70). Falsifiable claim: executing the status plan verbatim freezes before `phase4_mandatory` or an ANN sweep has run under Stella.

8. **MAJOR — sign flipping is not a valid test of the weak macro-mean null without an unverified symmetry assumption.** [boot.py:50](/home/dylan/asymetric-dual-encoders/m7src/boot.py:50) assumes each fixed-system per-query difference is symmetric/exchangeable about zero. The confirmatory claim is instead that the mean of dataset means is positive; deterministic retrieval systems were not randomly assigned to queries. The calibration at [test_signflip_calibration.py:29](/home/dylan/asymetric-dual-encoders/m7src/test_signflip_calibration.py:29) constructs exchangeability by random swapping and therefore only verifies the assumption it imposed. Falsifiable claim: a bounded, asymmetric zero-mean difference distribution at the same stratum sizes can make this unstudentized sign-flip test exceed nominal type-I error, especially in the 50-query stratum.

9. **MAJOR — M-perquery remains independently validated for BM25 only, not for two decisive comparators.** The validator admits that its hashes were generated from the already-existing file after the original caches were lost at [validate_perquery.py:7](/home/dylan/asymetric-dual-encoders/scripts/validate_perquery.py:7); only BM25 is independently recomputed at [validate_perquery.py:68](/home/dylan/asymetric-dual-encoders/scripts/validate_perquery.py:68). Falsifiable claim: an LR or OpenSearch vector that was already permuted before `perquery.sha256.json` was created passes every current provenance check, yet C1 or C3 pairing is wrong.

10. **MAJOR — “the KL set is clean by construction” is false for its positive member.** `build_arrays` filters provided hard negatives but not `pos_idx` at [train.py:95](/home/dylan/asymetric-dual-encoders/m7src/train.py:95); every KL candidate set then starts with `pool_vecs[p_i]` at [train.py:503](/home/dylan/asymetric-dual-encoders/m7src/train.py:503). Falsifiable claim: any training positive that is banned because its document contains a six/untouched query gram still enters both KL and InfoNCE as a positive.

11. **MAJOR — the row mask and mining caches are not bound to complete input identity.** `banned_rows()` validates neither pool row order nor pool metadata at [train.py:115](/home/dylan/asymetric-dual-encoders/m7src/train.py:115); teacher mining hashes only every 997th query vector and the pool shape at [train.py:203](/home/dylan/asymetric-dual-encoders/m7src/train.py:203), while BM25 mining omits exclusions and corpus identity at [train.py:145](/home/dylan/asymetric-dual-encoders/m7src/train.py:145). Falsifiable claim: reorder a same-sized pool or alter an unsampled query/exclusion while preserving names and shapes, and stale masks/mining arrays are accepted.

12. **MAJOR — miner fallbacks can reintroduce positives as negatives.** The BM25 fallback at [train.py:175](/home/dylan/asymetric-dual-encoders/m7src/train.py:175) and teacher fallback at [train.py:240](/home/dylan/asymetric-dual-encoders/m7src/train.py:240) seek only an unbanned row; they do not exclude that query’s positives. Falsifiable claim: when all retrieved candidates are filtered, a query whose positive is the fallback row receives its own positive as a hard negative.

13. **MAJOR — the preregistered best-eval rule is not implemented on the declared full dev macro.** Stella arms inherit the three-component proxy at [train.py:68](/home/dylan/asymetric-dual-encoders/m7src/train.py:68); training records intermediate scores but saves only the final table at [train.py:608](/home/dylan/asymetric-dual-encoders/m7src/train.py:608). `grid()` returns final macros, while [RESULTS.md:6](/home/dylan/asymetric-dual-encoders/m7/RESULTS.md:6) says every selection uses the full six-component suite. Falsifiable claim: if an arm peaks at step 500, current files contain no step-500 table to evaluate on full dev or freeze.

14. **MAJOR — Stella’s teacher-selection dev set is not exposure-free.** The ledger calls the two CQADupStack components absent from candidates’ disclosed training at [LEDGER.md:216](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:216), but Stella discloses `StackExchangeClusteringP2P` and related StackExchange datasets at [m7_teacher_contamination.json:43](/home/dylan/asymetric-dual-encoders/results/m7_teacher_contamination.json:43). That is the same source family and pair-to-pair task shape as CQADupStack. A cheap skipped check was to source-stratify the already reserved held-out SQuAD/ESCI slices and rank the closed-form tables on those small stores before the 6.17M encode. Falsifiable claim: if Stella does not remain first off StackExchange, the chosen advantage was family-specific.

15. **MAJOR — Qwen3 and Granite were closed using the criterion this session refuted.** [EXPLORED.md:18](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:18) rejects MRL-256 Qwen3 on full-dimensional symmetric MTEB, and [EXPLORED.md:20](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:20) rejects Granite on the same anchor plus a 50,368-token vocabulary. Yet [EXPLORED.md:24](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:24) says symmetric quality has zero rank correlation with table quality, while [CLAUDE.md:130](/home/dylan/asymetric-dual-encoders/CLAUDE.md:130) allows approximately 50K vocab or MRL. Falsifiable claim: neither candidate has been evaluated by the adopted trained/closed-form table criterion, so “dominated” is unsupported.

16. **MAJOR — “clean-4” overstates what is known.** [m7_teacher_contamination.json:2](/home/dylan/asymetric-dual-encoders/results/m7_teacher_contamination.json:2) explicitly says absent registry entries are not evidence of cleanliness, while [m7_bars_clean4.json:2](/home/dylan/asymetric-dual-encoders/results/m7_bars_clean4.json:2) defines the subset only by absence of disclosed exact benchmark names. Falsifiable claim: the defensible label is “four datasets without disclosed direct benchmark overlap,” not “clean”; Stella’s disclosed ArXiv/BioRxiv/FEVER-family training still creates source/domain exposure concerns.

17. **MAJOR — the preregistered clean-4 robustness analysis is absent from the final scorer.** [final_run.py:180](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:180) computes only six-set rows and the three six-set comparisons; no clean-4 macro, CI, comparator, or exposure label is written through [final_run.py:225](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:225). Falsifiable claim: the current `m7_final_run.json` schema cannot contain the promised robustness decision without post-final analysis code.

18. **MAJOR — android/english are not genuinely untouched with respect to model development.** They are held-out subforums from the same benchmark family used repeatedly for teacher selection, ridge-lambda selection, training selection, and fusion, as acknowledged at [LEDGER.md:113](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:113); Stella additionally discloses StackExchange-family training. Falsifiable claim: these rows measure within-family/subforum transfer, not untouched cross-family generalization. “Development-informed at family level” is accurate only if they are not marketed as repairing the untouched-generalization partition.

19. **MAJOR — the canonical convex semantics discard singleton BM25 evidence.** After padding is dropped, [fusion.py:27](/home/dylan/asymetric-dual-encoders/m7src/fusion.py:27) min-max normalizes only positive hits. For a query with exactly one BM25 match, `hi == lo`, so that document receives zero BM25 contribution—identical to an absent document. Falsifiable claim: restoring a zero-score missing-document baseline gives the singleton a maximal lexical contribution and can select a different fusion family/weight; this should be compared on dev before freezing.

20. **MAJOR — the permitted infrastructure retry cannot pass its own guard.** The guard requires a clean tree at [final_run.py:68](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:68), but the initial attempt appends to tracked `m7/LEDGER.md` before scoring at [final_run.py:172](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:172). Committing that ledger change alters HEAD away from the freeze hash. Falsifiable claim: after any post-BEGIN crash, `--infra-retry` fails either the clean-tree check or the frozen-HEAD check.

21. **MAJOR — the binding top-level decision record names two active teachers.** [CLAUDE.md:208](/home/dylan/asymetric-dual-encoders/CLAUDE.md:208) withdraws Arctic and selects Stella on table learnability, but [CLAUDE.md:218](/home/dylan/asymetric-dual-encoders/CLAUDE.md:218) immediately states, without a withdrawn/superseded marker, “Teacher for M7 is Arctic.” Falsifiable claim: a fresh session following the binding M7 bullet can legitimately choose the wrong teacher.

22. **MINOR — ties/zeros and Monte Carlo-floor reporting are misleading.** `signflip` reports total `n`, not nonzero differences, at [boot.py:77](/home/dylan/asymetric-dual-encoders/m7src/boot.py:77), so TREC-COVID may have substantially fewer than 50 informative pairs. The same line prints `<1/(R+1)` when the returned add-one p equals exactly `1/(R+1)`. Falsifiable claim: with 49 zero TREC differences, the output still reports `n=50`; at zero exceedances it reports a strict upper bound the calculation did not establish.

23. **MINOR — the committed phase-2 significance artifact still contains the invalid pre-B3 p-values.** [m7_phase2_screen_cis.json:26](/home/dylan/asymetric-dual-encoders/results/m7_phase2_screen_cis.json:26) retains bootstrap `p` fields and [m7_phase2_screen_cis.json:341](/home/dylan/asymetric-dual-encoders/results/m7_phase2_screen_cis.json:341) retains Holm decisions over them, despite the code now declaring those tail masses invalid. Falsifiable claim: a consumer reading the committed artifact—not the updated script—still receives the superseded inference as authoritative.

24. **MAJOR — the freeze plan abandons every known capacity-adding route despite the remaining gap.** [CLAUDE.md:95](/home/dylan/asymetric-dual-encoders/CLAUDE.md:95) says only n-gram rows and multiplicity-dependent pooling add capacity; [EXPLORED.md:32](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:32) also reopens a cheap doc2query experiment, and [program.py:239](/home/dylan/asymetric-dual-encoders/m7src/program.py:239) defines an unused pseudo-query phase. None appears in `RESULTS.md` or before freeze in `STATUS.md`. Falsifiable claim: the current plan attempts to bridge a roughly 0.047 release-bar gap using only a previously observed ≈0.011 contrastive gain, without testing any lever known to add representational capacity.
tokens used
410,666
One-line positive: the one-sided directions, add-one Monte Carlo construction, and three-test Holm plumbing are internally coherent if the sharp exchangeability null is accepted.

1. **BLOCKER — the B2 bank mask can make training index past the end of the bank.** [train.py:408](/home/dylan/asymetric-dual-encoders/m7src/train.py:408) sets `nb`, removes banned `bank_ids` without updating it, then samples indices from `[0, nb)` at [train.py:515](/home/dylan/asymetric-dual-encoders/m7src/train.py:515) and [train.py:538](/home/dylan/asymetric-dual-encoders/m7src/train.py:538). Falsifiable claim: if even one of the sampled two million rows is banned, some legal sampled indices exceed `len(bank)-1`, eventually causing an out-of-range CUDA index.

2. **BLOCKER — the folded release artifact is not on the gate/freeze/final path.** `save_release` is defined at [table.py:200](/home/dylan/asymetric-dual-encoders/m7src/table.py:200) but has no caller; G4 loads the ordinary unfolded checkpoint at [gate.py:34](/home/dylan/asymetric-dual-encoders/m7src/gate.py:34), and `freeze.write` freezes that same `work/runs/<id>.npz` at [freeze.py:36](/home/dylan/asymetric-dual-encoders/m7src/freeze.py:36). Falsifiable claim: following `STATUS.md` today never creates, gates, freezes, or scores the folded artifact despite [STATUS.md:20](/home/dylan/asymetric-dual-encoders/m7/STATUS.md:20) claiming otherwise. Also, `save_release` compares two fp32 in-memory models before writing; it never reloads either serialized fp16 or int8 variant.

3. **BLOCKER — the Stella re-gate silently reuses the BGE capacity probe.** [gate.py:91](/home/dylan/asymetric-dual-encoders/m7src/gate.py:91) always reads the generic `m7_capacity_probe_noprefix.json`, whose contents carry no teacher identity at [m7_capacity_probe_noprefix.json:1](/home/dylan/asymetric-dual-encoders/results/m7_capacity_probe_noprefix.json:1). Falsifiable claim: `M7_ENCODER=stella-400M-v5 gate.py s1-objB` can pass G2 using the old 768-dimensional BGE document space without detecting the mismatch.

4. **BLOCKER — the “4-gram short-query fix” still misses short queries embedded in normal documents.** Protected 4–7-word queries contribute word-4-grams at [decontam_pool.py:49](/home/dylan/asymetric-dual-encoders/m7src/decontam_pool.py:49), but a pool document contributes 4-grams only when the document itself has fewer than eight words at [decontam_pool.py:108](/home/dylan/asymetric-dual-encoders/m7src/decontam_pool.py:108). Falsifiable claim: a 20-word pool document containing a protected five-word query verbatim produces only rolling 8-grams and is not banned. One-to-three-word queries remain whole-document-exact only.

5. **BLOCKER — final tier assignment violates the binding CI rule.** The mandate says tiers are decided only by paired-bootstrap CIs and unresolved comparisons fall lower at [instructions-m7.md:28](/home/dylan/asymetric-dual-encoders/instructions-m7.md:28); [final_run.py:205](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:205) and [final_run.py:227](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:227) assign rejections/tiers solely from sign-flip Holm decisions. Falsifiable claim: a Holm rejection with a percentile CI crossing zero is currently reported as a tier win, contrary to the mandate.

6. **BLOCKER — the six-set access breach is not curable by disclosure under the project’s own norms.** The mandate limits class-(a) access to three named harness cells at [instructions-m7.md:66](/home/dylan/asymetric-dual-encoders/instructions-m7.md:66); the ledger admits the FiQA throughput breach at [LEDGER.md:283](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:283), and [SIX_ACCESS.log:1](/home/dylan/asymetric-dual-encoders/m7/SIX_ACCESS.log:1) records another full-six BM25 recomputation outside that named validation. [CLAUDE.md:146](/home/dylan/asymetric-dual-encoders/CLAUDE.md:146) forbids retroactive protocol relaxation. Falsifiable claim: an audit log proves access occurred; it cannot make the earlier accesses authorized or restore a “single final access” claim.

7. **BLOCKER — the recorded next-step order reaches freeze before mandatory work.** [STATUS.md:26](/home/dylan/asymetric-dual-encoders/m7/STATUS.md:26) schedules objective B, the LR band, fusion, then freeze, omitting the three inits, two prefix variants, flat-vs-learned weights, released-table ANN sweep, and cost measurements required at [instructions-m7.md:70](/home/dylan/asymetric-dual-encoders/instructions-m7.md:70). Falsifiable claim: executing the status plan verbatim freezes before `phase4_mandatory` or an ANN sweep has run under Stella.

8. **MAJOR — sign flipping is not a valid test of the weak macro-mean null without an unverified symmetry assumption.** [boot.py:50](/home/dylan/asymetric-dual-encoders/m7src/boot.py:50) assumes each fixed-system per-query difference is symmetric/exchangeable about zero. The confirmatory claim is instead that the mean of dataset means is positive; deterministic retrieval systems were not randomly assigned to queries. The calibration at [test_signflip_calibration.py:29](/home/dylan/asymetric-dual-encoders/m7src/test_signflip_calibration.py:29) constructs exchangeability by random swapping and therefore only verifies the assumption it imposed. Falsifiable claim: a bounded, asymmetric zero-mean difference distribution at the same stratum sizes can make this unstudentized sign-flip test exceed nominal type-I error, especially in the 50-query stratum.

9. **MAJOR — M-perquery remains independently validated for BM25 only, not for two decisive comparators.** The validator admits that its hashes were generated from the already-existing file after the original caches were lost at [validate_perquery.py:7](/home/dylan/asymetric-dual-encoders/scripts/validate_perquery.py:7); only BM25 is independently recomputed at [validate_perquery.py:68](/home/dylan/asymetric-dual-encoders/scripts/validate_perquery.py:68). Falsifiable claim: an LR or OpenSearch vector that was already permuted before `perquery.sha256.json` was created passes every current provenance check, yet C1 or C3 pairing is wrong.

10. **MAJOR — “the KL set is clean by construction” is false for its positive member.** `build_arrays` filters provided hard negatives but not `pos_idx` at [train.py:95](/home/dylan/asymetric-dual-encoders/m7src/train.py:95); every KL candidate set then starts with `pool_vecs[p_i]` at [train.py:503](/home/dylan/asymetric-dual-encoders/m7src/train.py:503). Falsifiable claim: any training positive that is banned because its document contains a six/untouched query gram still enters both KL and InfoNCE as a positive.

11. **MAJOR — the row mask and mining caches are not bound to complete input identity.** `banned_rows()` validates neither pool row order nor pool metadata at [train.py:115](/home/dylan/asymetric-dual-encoders/m7src/train.py:115); teacher mining hashes only every 997th query vector and the pool shape at [train.py:203](/home/dylan/asymetric-dual-encoders/m7src/train.py:203), while BM25 mining omits exclusions and corpus identity at [train.py:145](/home/dylan/asymetric-dual-encoders/m7src/train.py:145). Falsifiable claim: reorder a same-sized pool or alter an unsampled query/exclusion while preserving names and shapes, and stale masks/mining arrays are accepted.

12. **MAJOR — miner fallbacks can reintroduce positives as negatives.** The BM25 fallback at [train.py:175](/home/dylan/asymetric-dual-encoders/m7src/train.py:175) and teacher fallback at [train.py:240](/home/dylan/asymetric-dual-encoders/m7src/train.py:240) seek only an unbanned row; they do not exclude that query’s positives. Falsifiable claim: when all retrieved candidates are filtered, a query whose positive is the fallback row receives its own positive as a hard negative.

13. **MAJOR — the preregistered best-eval rule is not implemented on the declared full dev macro.** Stella arms inherit the three-component proxy at [train.py:68](/home/dylan/asymetric-dual-encoders/m7src/train.py:68); training records intermediate scores but saves only the final table at [train.py:608](/home/dylan/asymetric-dual-encoders/m7src/train.py:608). `grid()` returns final macros, while [RESULTS.md:6](/home/dylan/asymetric-dual-encoders/m7/RESULTS.md:6) says every selection uses the full six-component suite. Falsifiable claim: if an arm peaks at step 500, current files contain no step-500 table to evaluate on full dev or freeze.

14. **MAJOR — Stella’s teacher-selection dev set is not exposure-free.** The ledger calls the two CQADupStack components absent from candidates’ disclosed training at [LEDGER.md:216](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:216), but Stella discloses `StackExchangeClusteringP2P` and related StackExchange datasets at [m7_teacher_contamination.json:43](/home/dylan/asymetric-dual-encoders/results/m7_teacher_contamination.json:43). That is the same source family and pair-to-pair task shape as CQADupStack. A cheap skipped check was to source-stratify the already reserved held-out SQuAD/ESCI slices and rank the closed-form tables on those small stores before the 6.17M encode. Falsifiable claim: if Stella does not remain first off StackExchange, the chosen advantage was family-specific.

15. **MAJOR — Qwen3 and Granite were closed using the criterion this session refuted.** [EXPLORED.md:18](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:18) rejects MRL-256 Qwen3 on full-dimensional symmetric MTEB, and [EXPLORED.md:20](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:20) rejects Granite on the same anchor plus a 50,368-token vocabulary. Yet [EXPLORED.md:24](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:24) says symmetric quality has zero rank correlation with table quality, while [CLAUDE.md:130](/home/dylan/asymetric-dual-encoders/CLAUDE.md:130) allows approximately 50K vocab or MRL. Falsifiable claim: neither candidate has been evaluated by the adopted trained/closed-form table criterion, so “dominated” is unsupported.

16. **MAJOR — “clean-4” overstates what is known.** [m7_teacher_contamination.json:2](/home/dylan/asymetric-dual-encoders/results/m7_teacher_contamination.json:2) explicitly says absent registry entries are not evidence of cleanliness, while [m7_bars_clean4.json:2](/home/dylan/asymetric-dual-encoders/results/m7_bars_clean4.json:2) defines the subset only by absence of disclosed exact benchmark names. Falsifiable claim: the defensible label is “four datasets without disclosed direct benchmark overlap,” not “clean”; Stella’s disclosed ArXiv/BioRxiv/FEVER-family training still creates source/domain exposure concerns.

17. **MAJOR — the preregistered clean-4 robustness analysis is absent from the final scorer.** [final_run.py:180](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:180) computes only six-set rows and the three six-set comparisons; no clean-4 macro, CI, comparator, or exposure label is written through [final_run.py:225](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:225). Falsifiable claim: the current `m7_final_run.json` schema cannot contain the promised robustness decision without post-final analysis code.

18. **MAJOR — android/english are not genuinely untouched with respect to model development.** They are held-out subforums from the same benchmark family used repeatedly for teacher selection, ridge-lambda selection, training selection, and fusion, as acknowledged at [LEDGER.md:113](/home/dylan/asymetric-dual-encoders/m7/LEDGER.md:113); Stella additionally discloses StackExchange-family training. Falsifiable claim: these rows measure within-family/subforum transfer, not untouched cross-family generalization. “Development-informed at family level” is accurate only if they are not marketed as repairing the untouched-generalization partition.

19. **MAJOR — the canonical convex semantics discard singleton BM25 evidence.** After padding is dropped, [fusion.py:27](/home/dylan/asymetric-dual-encoders/m7src/fusion.py:27) min-max normalizes only positive hits. For a query with exactly one BM25 match, `hi == lo`, so that document receives zero BM25 contribution—identical to an absent document. Falsifiable claim: restoring a zero-score missing-document baseline gives the singleton a maximal lexical contribution and can select a different fusion family/weight; this should be compared on dev before freezing.

20. **MAJOR — the permitted infrastructure retry cannot pass its own guard.** The guard requires a clean tree at [final_run.py:68](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:68), but the initial attempt appends to tracked `m7/LEDGER.md` before scoring at [final_run.py:172](/home/dylan/asymetric-dual-encoders/m7src/final_run.py:172). Committing that ledger change alters HEAD away from the freeze hash. Falsifiable claim: after any post-BEGIN crash, `--infra-retry` fails either the clean-tree check or the frozen-HEAD check.

21. **MAJOR — the binding top-level decision record names two active teachers.** [CLAUDE.md:208](/home/dylan/asymetric-dual-encoders/CLAUDE.md:208) withdraws Arctic and selects Stella on table learnability, but [CLAUDE.md:218](/home/dylan/asymetric-dual-encoders/CLAUDE.md:218) immediately states, without a withdrawn/superseded marker, “Teacher for M7 is Arctic.” Falsifiable claim: a fresh session following the binding M7 bullet can legitimately choose the wrong teacher.

22. **MINOR — ties/zeros and Monte Carlo-floor reporting are misleading.** `signflip` reports total `n`, not nonzero differences, at [boot.py:77](/home/dylan/asymetric-dual-encoders/m7src/boot.py:77), so TREC-COVID may have substantially fewer than 50 informative pairs. The same line prints `<1/(R+1)` when the returned add-one p equals exactly `1/(R+1)`. Falsifiable claim: with 49 zero TREC differences, the output still reports `n=50`; at zero exceedances it reports a strict upper bound the calculation did not establish.

23. **MINOR — the committed phase-2 significance artifact still contains the invalid pre-B3 p-values.** [m7_phase2_screen_cis.json:26](/home/dylan/asymetric-dual-encoders/results/m7_phase2_screen_cis.json:26) retains bootstrap `p` fields and [m7_phase2_screen_cis.json:341](/home/dylan/asymetric-dual-encoders/results/m7_phase2_screen_cis.json:341) retains Holm decisions over them, despite the code now declaring those tail masses invalid. Falsifiable claim: a consumer reading the committed artifact—not the updated script—still receives the superseded inference as authoritative.

24. **MAJOR — the freeze plan abandons every known capacity-adding route despite the remaining gap.** [CLAUDE.md:95](/home/dylan/asymetric-dual-encoders/CLAUDE.md:95) says only n-gram rows and multiplicity-dependent pooling add capacity; [EXPLORED.md:32](/home/dylan/asymetric-dual-encoders/m7/EXPLORED.md:32) also reopens a cheap doc2query experiment, and [program.py:239](/home/dylan/asymetric-dual-encoders/m7src/program.py:239) defines an unused pseudo-query phase. None appears in `RESULTS.md` or before freeze in `STATUS.md`. Falsifiable claim: the current plan attempts to bridge a roughly 0.047 release-bar gap using only a previously observed ≈0.011 contrastive gain, without testing any lever known to add representational capacity.
